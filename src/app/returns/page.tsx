export default function ReturnsPage() {
  return (
    <main className="support-page">
      <section className="support-hero">
        <p className="categories-eyebrow">
          CUSTOMER SUPPORT
        </p>

        <h1>Returns</h1>

        <p>
          Learn more about our return policy, eligible products,
          and how to start a return.
        </p>
      </section>
    </main>
  );
}